@model HomeCatalogEmptyModel
<div class="empty-state-card" data-testid="@Model.TestId">
    <h3>@Model.Title</h3>
    <p class="meta">
        Repositories appear here once your forge admin creates them.
        Power users: press <code>Ctrl+K</code> → <code>/repo create</code>.
    </p>
</div>
