# Публикация AIGuiders.UI.* на nuget.org через Trusted Publishing (OIDC).
# nuget.org: Trusted Publishing → Package owner AIGuiders, repo guiders-ui-platform, workflow release.yml
name: release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - run: dotnet test -c Release

      - run: dotnet pack -c Release --output ./artifacts

      - name: NuGet login (OIDC → временный API key)
        uses: NuGet/login@v1
        id: nuget_login
        with:
          user: AIGuiders

      - name: Push nupkg / snupkg
        env:
          NUGET_API_KEY: ${{ steps.nuget_login.outputs.NUGET_API_KEY }}
        run: bash scripts/nuget/push-artifacts.sh 'artifacts/AIGuiders.UI.*.nupkg'
