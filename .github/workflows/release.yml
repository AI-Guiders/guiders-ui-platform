# Публикация AIGuiders.UI.* на nuget.org через Trusted Publishing (OIDC).
# nuget.org (LonelySoul): Trusted Publishing → owner AI-Guiders, repo guiders-ui-platform, workflow release.yml
name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - run: dotnet test -c Release

      - run: dotnet pack -c Release --output ./artifacts

      - name: NuGet login (OIDC → временный API key)
        uses: NuGet/login@v1
        id: nuget_login
        with:
          user: LonelySoul

      - name: Push nupkg
        shell: bash
        run: |
          set -euo pipefail
          key="${{ steps.nuget_login.outputs.NUGET_API_KEY }}"
          src="https://api.nuget.org/v3/index.json"
          shopt -s nullglob
          files=(artifacts/AIGuiders.UI.*.nupkg)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No AIGuiders.UI.*.nupkg under artifacts/"
            exit 1
          fi
          for f in "${files[@]}"; do
            echo "push $f"
            dotnet nuget push "$f" --api-key "$key" --source "$src" --skip-duplicate
          done
